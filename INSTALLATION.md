# KoalaSnap 安装指南

## 📋 安装前准备

确保你已经完成以下准备：
- ✅ Chrome 浏览器（版本 88 或更高）
- ✅ 下载或克隆了本项目

## 🚀 详细安装步骤

### 步骤 1: 打开扩展管理页面

在 Chrome 浏览器地址栏输入：
```
chrome://extensions/
```

或者通过菜单打开：
1. 点击浏览器右上角的 **三个点** 菜单
2. 选择 **更多工具** → **扩展程序**

### 步骤 2: 开启开发者模式

在扩展管理页面的右上角，找到并开启 **「开发者模式」** 开关。

![开发者模式](https://developer.chrome.com/static/docs/extensions/mv3/getstarted/images/devmode.png)

### 步骤 3: 加载扩展

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮
2. 在弹出的文件选择对话框中，导航到本项目的根目录：
   ```
   d:\同步\源码\开发中\截图插件
   ```
3. 选择该文件夹并点击 **「选择文件夹」**

### 步骤 4: 验证安装

安装成功后，你会看到：
- ✅ 扩展列表中出现 **「KoalaSnap - 考拉易截」**
- ✅ 浏览器工具栏出现蓝色相机图标
- ✅ 扩展状态显示为 **「已启用」**

## 🎯 快速测试

### 测试 1: 点击图标
1. 点击工具栏的扩展图标
2. 应该看到一个漂亮的弹出窗口，包含三个截图模式按钮

### 测试 2: 可见区域截图
1. 打开任意网页（如 https://www.google.com）
2. 点击扩展图标
3. 点击 **「可见区域」** 按钮
4. 检查下载文件夹，应该有一个新的 PNG 截图

### 测试 3: 快捷键
1. 打开任意网页
2. 按 `Ctrl+Shift+S` (Mac: `Cmd+Shift+S`)
3. 应该自动截取可见区域并保存

### 测试 4: 整页截图
1. 打开一个长页面（如 https://www.wikipedia.org）
2. 点击扩展图标
3. 点击 **「整页截图」** 按钮
4. 等待进度条完成
5. 检查下载的长截图

### 测试 5: 区域选择
1. 打开任意网页
2. 点击扩展图标
3. 点击 **「区域选择」** 按钮
4. 在页面上拖动鼠标选择区域
5. 松开鼠标，截图自动保存

## ⚠️ 常见问题

### 问题 1: 扩展图标不显示
**解决方法：**
1. 点击浏览器工具栏的 **拼图图标**（扩展管理）
2. 找到 **「一键截图」**
3. 点击 **图钉图标** 将其固定到工具栏

### 问题 2: 快捷键不工作
**解决方法：**
1. 访问 `chrome://extensions/shortcuts`
2. 找到 **「一键截图」**
3. 检查快捷键设置，如有冲突请修改

### 问题 3: 某些页面无法截图
**原因：**
Chrome 内部页面（如 `chrome://`、`chrome-extension://`）出于安全限制无法截图。

**解决方法：**
在普通网页上使用扩展。

### 问题 4: 截图模糊
**原因：**
可能是高 DPI 屏幕适配问题。

**解决方法：**
本扩展已支持高 DPI，如仍模糊请检查浏览器缩放设置（应为 100%）。

## 🔧 开发调试

### 查看控制台日志

**Background Script 日志：**
1. 在 `chrome://extensions/` 找到本扩展
2. 点击 **「检查视图」** 下的 `service worker`
3. 打开开发者工具查看日志

**Popup 日志：**
1. 点击扩展图标打开 popup
2. 右键点击 popup 窗口
3. 选择 **「检查」**
4. 查看控制台日志

**Content Script 日志：**
1. 在任意网页按 `F12` 打开开发者工具
2. 查看控制台中的 "Screenshot Extension Content Script Loaded" 消息

### 重新加载扩展

修改代码后需要重新加载：
1. 访问 `chrome://extensions/`
2. 找到 **「一键截图」**
3. 点击 **刷新图标** 🔄

## 📦 文件结构检查

确保项目包含以下文件：

```
截图插件/
├── manifest.json          ✅ 必需
├── background.js          ✅ 必需
├── content.js            ✅ 必需
├── popup/
│   ├── popup.html        ✅ 必需
│   ├── popup.css         ✅ 必需
│   └── popup.js          ✅ 必需
├── icons/
│   ├── icon-16.png       ✅ 必需
│   ├── icon-48.png       ✅ 必需
│   └── icon-128.png      ✅ 必需
└── README.md             ℹ️ 可选
```

## 🎉 安装成功！

现在你可以：
- 🖼️ 使用三种截图模式
- ⌨️ 使用快捷键快速截图
- 📁 在下载文件夹找到所有截图

享受你的新截图工具吧！

---

**需要帮助？** 查看 [README.md](README.md) 获取更多信息。
